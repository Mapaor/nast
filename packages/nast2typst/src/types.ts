/**
 * NAST to Typst Type Definitions
 *
 * Re-exports canonical types from @nast/types for backward compatibility.
 */

// Re-export all NAST types
export type {
  NASTRoot,
  NASTNode,
  NASTParagraph,
  NASTHeading,
  NASTText,
  NASTStrong,
  NASTEmphasis,
  NASTUnderline,
  NASTDelete,
  NASTInlineCode,
  NASTLink,
  NASTMention,
  NASTMath,
  NASTInlineMath,
  NASTCode,
  NASTBlockquote,
  NASTCallout,
  NASTToggle,
  NASTList,
  NASTListItem,
  NASTColumnList,
  NASTColumn,
  NASTImage,
  NASTThematicBreak,
  NASTTable,
  NASTTableRow,
  NASTTableCell,
  NASTChildPage,
  NASTVideo,
  NASTFile,
  NASTPDF,
  NASTBookmark,
  NASTEmbed,
} from '@nast/types';
