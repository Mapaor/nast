/**
 * Layout node types re-exported from @nast/types for backward compatibility
 */
export type {
  NBTColumnListNode as ColumnListNode,
  NBTColumnNode as ColumnNode,
} from '@nast/types';

// Re-export NotionBlock for backward compatibility
export type { NBTBlock as NotionBlock } from '@nast/types';
