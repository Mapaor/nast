/**
 * Page node types re-exported from @nast/types for backward compatibility
 */
export type {
  NBTPageNode as PageNode,
  NBTPageProperties as PageProperties,
} from '@nast/types';

// Re-export NotionBlock for backward compatibility
export type { NBTBlock as NotionBlock } from '@nast/types';
