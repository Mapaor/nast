## About thi package

This package fetches the URLs from a NAST JSON file (as input) and fetches all the images and returns them as bytes.

NOTE: The NAST must have been obtained recently otherwise image links may have expired

